Headless mode allows the wiki engine to run in an API-only mode. This allows custom frontend to be implemented on top of the wiki engine. Enabling headless mode essentially disables the web frontend.
